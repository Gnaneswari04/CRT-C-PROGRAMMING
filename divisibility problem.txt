#include <stdio.h>

int main() {
   int a,b;
   scanf("%d %d",&a,&b);
   if(a%b==0){
   printf("0");
   }
   else
  {
      int count=0;
      while(a%b!=0){
          a=a+1;
          count++;
      }
      printf("%d",count);
  }
return 0;
}